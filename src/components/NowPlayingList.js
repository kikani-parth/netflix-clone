import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const NowPlayingList = () => {
  return (
    <View>
      <Text>NowPlayingList</Text>
    </View>
  );
};

export default NowPlayingList;

const styles = StyleSheet.create({});
